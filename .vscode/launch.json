{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/docker-test/basic-mmo-phaser-master/package.json",
                "localRoot": "${workspaceFolder}/docker-test/basic-mmo-phaser-master"
            }
        }
    ]
}